"""CellFate-Rx: predicting cellular outcomes of perturbations.

The foundation (this distribution) provides ``cellfate.common`` -- the shared
contract layer that every other package imports. The data, models, training,
inference and evaluation packages are specified in Documents 2-5 and build on it.
"""

__version__ = "0.1.0"
